#这节讲的是自定义模块的导入，希望学会
#一下内容均为自定义模块

def sortwords(word):
	return (sorted(word))
def breakword(stuff):
		words=stuff.split("")
		return (words)
def printfirstwords(word):
		words=word.pop(-1)
		print (wordss)

